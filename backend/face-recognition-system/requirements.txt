# Detecção e reconhecimento facial
insightface
onnxruntime          # CPU — troca por onnxruntime-gpu se tiver CUDA

# Visão computacional
opencv-python
numpy

# Busca vetorial (FAISS)
faiss-cpu            # CPU — troca por faiss-gpu se tiver CUDA

# Configuração
pyyaml               # leitura do config.yaml

# API (quando for implementar)
fastapi
uvicorn[standard]
python-multipart     # upload de imagens via form-data

# Utilitários
scikit-learn         # métricas, ROC curve para calibrar threshold